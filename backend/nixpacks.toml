[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x", "python3", "gcc", "gnumake"]
aptPkgs = ["build-essential"]

[phases.install]
cmds = ["npm install --build-from-source"]

[phases.build]
cmds = ["npm rebuild @swc/core", "npm run build"]

[start]
cmd = "npm run start"
